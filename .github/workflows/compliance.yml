name: Compliance Scan

on:
  push:
    branches:
      - main

jobs:
  compliance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install checkov pyyaml

      - name: Run Checkov JSON scan
        run: |
          checkov -d terraform --output json > checkov_results.json || true

      - name: Run compliance evaluation engine
        run: |
          python3 evaluate_controls.py

      - name: Upload compliance report
        uses: actions/upload-artifact@v4
        with:
          name: compliance-report
          path: evidence/week2_compliance_report.txt
